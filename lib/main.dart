import 'package:flutter/material.dart';

void main() {
  runApp(const Movura());
}

class Movura extends StatelessWidget {
  const Movura({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
